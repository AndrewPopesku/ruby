<%= form_with(model: payment_method, local: true, class: "mb-4") do |form| %>
  <% if payment_method.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(payment_method.errors.count, "error") %> prohibited this payment method from being saved:</h4>
      <ul>
        <% payment_method.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %> 